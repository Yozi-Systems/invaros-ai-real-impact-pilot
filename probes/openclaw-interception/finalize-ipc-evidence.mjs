#!/usr/bin/env node
import {createHash} from "node:crypto";import {execFileSync} from "node:child_process";import fs from "node:fs";import path from "node:path";
const evidence=process.argv[2],daemon=process.argv[3],pilot=process.cwd(),core="/home/yozi/invarosd";if(!daemon)throw new Error("usage: finalize EVIDENCE DAEMON");
const run=(cwd,...args)=>execFileSync(args[0],args.slice(1),{cwd,encoding:"utf8"}),sha=p=>createHash("sha256").update(fs.readFileSync(p)).digest("hex");
const files=[daemon,path.join(core,"schemas/openclaw_authorization.v1.schema.json"),path.join(pilot,"probes/openclaw-interception/index.ts"),path.join(pilot,"probes/openclaw-interception/dist/index.js"),path.join(pilot,"probes/openclaw-interception/protocol.ts"),path.join(pilot,"probes/openclaw-interception/ipc-client.ts"),path.join(pilot,"probes/openclaw-interception/run-ipc-vertical-slice.mjs")];
fs.writeFileSync(path.join(evidence,"source-binary-sha256.json"),JSON.stringify(Object.fromEntries(files.map(p=>[p,sha(p)])),null,2)+"\n");
fs.writeFileSync(path.join(evidence,"repository-state.txt"),`invarosd_head=${run(core,"git","rev-parse","HEAD").trim()}\ninvarosd_status:\n${run(core,"git","status","--short","--branch")}pilot_head=${run(pilot,"git","rev-parse","HEAD").trim()}\npilot_status:\n${run(pilot,"git","status","--short","--branch")}\n`);
const a=JSON.parse(fs.readFileSync(path.join(evidence,"assertion-summary.json"),"utf8"));
fs.writeFileSync(path.join(evidence,"EXECUTION_REPORT.md"),`# OpenClaw authorization IPC process evidence\n\nAll ${Object.keys(a.assertions).length} recorded assertions passed against real invarosd PID ${a.processes.daemonPid} and client-harness PID ${a.processes.harnessPid}. DENY left the sentinel hash unchanged; ALLOW and the concurrent ALLOW produced the two recorded executions. Correlation, transmitted parameter digest/length, peer PID, duplicate replay, collision denial, stale denial, concurrency, cleanup, and daemon-unavailable failure passed.\n\nThis package does not contain a live OpenClaw Gateway HTTP run: deployment of the new plugin artifact was blocked by unavailable non-interactive sudo authentication. The historical native-hook evidence is separate. Logs are redacted operational evidence, not cryptographic or tamper-proof receipts.\n`);
